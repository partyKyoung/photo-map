import React from 'react';

import styled from 'styled-components';

const Wrapper = styled.div`
  padding: 1rem 0;
  text-align: center;
`;

const App: React.FC = () => <Wrapper>Photo map :)</Wrapper>;

export default App;
