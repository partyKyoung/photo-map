import React from 'react';

import UploadPhotoContainer from '@/containers/UploadPhotoContainer';

import PageTemplate from '@/components/templates/PageTemplate';

const UploadPhoto = () => {
  return (
    <PageTemplate>
      <UploadPhotoContainer />
    </PageTemplate>
  );
};

export default UploadPhoto;
